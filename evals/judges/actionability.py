"""LLM-as-judge: evaluates whether requirements are dissolved into implementation steps."""

PROMPT = """\
You are evaluating a specification generated by an AI skill that translates casual app \
requests into technical specs.

**Generated specification:**
{output}

Rate the ACTIONABILITY on a 1-5 scale. The key question: are behavioral requirements \
(validation rules, error handling, loading states, edge cases, interaction feedback) \
integrated as concrete sub-steps within numbered implementation steps, or do they exist \
only in separate descriptive sections that a builder might skip?

1 = Requirements are mostly in separate sections (Validation Rules, Error Handling) with \
no corresponding implementation steps. A builder following only the Features section would \
miss most of them.
2 = Some requirements appear in implementation steps, but most validation/error/edge-case \
details are in separate sections only.
3 = Mixed — about half of behavioral requirements appear in implementation steps, half in \
separate sections.
4 = Most requirements are in implementation steps. Separate sections exist as summaries. \
A builder following the Features section would catch most requirements.
5 = Every behavioral requirement has a corresponding implementation sub-step with specific \
instructions (what to build, what pattern to use). Separate sections are purely reference \
summaries.

Respond with ONLY valid JSON:
{{"score": <1-5>, "justification": "<2-3 sentences explaining the rating>"}}
"""

DIMENSION = "actionability"
