"""LLM-as-judge: evaluates how much meaningful technical depth was added."""

PROMPT = """\
You are evaluating a specification generated by an AI skill that translates casual app \
requests into technical specs.

**User's original request:**
{input}

**Generated specification:**
{output}

Rate the EXPANSION QUALITY on a 1-5 scale:

1 = Just reformatted or reorganized what the user already said. No new technical depth.
2 = Added a few obvious things (auth, basic validation) but missed most implicit requirements.
3 = Added standard technical requirements (auth flows, error handling, validation rules) but \
nothing the user wouldn't expect.
4 = Surfaced non-obvious requirements the user likely missed (edge cases, state management, \
error recovery, accessibility, specific UI patterns named).
5 = Comprehensive expansion — surfaced hidden decision trees, named specific interaction \
patterns, addressed security, identified architectural decisions the user didn't know they \
needed to make.

Respond with ONLY valid JSON:
{{"score": <1-5>, "justification": "<2-3 sentences explaining the rating>"}}
"""

DIMENSION = "expansion_quality"
